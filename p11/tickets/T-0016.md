---
id: T-0016
titel: "Die Frontend-Hälfte des Rückbaus: kachelFelder, dashboardGruppen, feldText und FELD_TITEL haben seit SWR-148 keinen Aufrufer"
typ: task
prozess: swe3
rolle: dev
sprint: 1
status: in_review
prio: niedrig
blocked_by: []
repo: p11
reviewer: qm
geändert: 2026-08-21
geplant_sprint: 28
erstellt: 2026-08-20
---

## Herkunft — der benannte Rand von `p11/T-0015`

`p11/T-0015` hat in Sprint 24 die **Backend**-Hälfte der Kachelanzeige zurückgebaut: die
Route `GET /api/dashboard`, `aggregation.dashboard` und `KACHEL_FELDER`. Ihre DoD nannte
diese drei und nichts sonst.

Beim Bauen ist der Rand aufgefallen und **gezählt** worden statt mitgenommen.

## Der Bestand — gemessen, nicht geschätzt

`regeln.js` (Stand 2026-08-20, nach dem Rückbau):

| Baustein | Aufrufer in `app.js` | Aufrufer in `regeln.js` |
|---|---|---|
| `FELD_TITEL` | 0 | nur `kachelFelder` |
| `feldText` | 0 | nur `kachelFelder` |
| `kachelFelder` | **0** | 0 |
| `dashboardGruppen` | **0** | 0 |

Dazu **11** von 111 JS-Zusicherungen in `regeln.test.cjs` (gemessen über einen Durchlauf
je Testblock nach `R.feldText` / `R.kachelFelder` / `R.dashboardGruppen`).

> **Seit SWR-148 zeichnet niemand mehr Kompaktkacheln. Der Code, der sie berechnet, ist
> seitdem tot — und `p11/T-0015` hat gerade die Datenquelle darunter entfernt.**

## ⚠ Warum das NICHT in `T-0015` mitgelaufen ist

Nicht aus Kapazität. Der Grund steht in `T-0015` selbst und ist ein Befund dieses Baus:

> **Ein Rückbau, der während seiner Ausführung wächst, ist genau der Rückbau, der etwas
> kaputt macht.**

`aggregation.zustaende_von` sah im selben Lauf aus wie Dashboard-Code und trägt seit
SWR-146 den `zustaende`-Block des **Cockpits**. Wäre sie „im Vorbeigehen" mitgegangen,
hätte eine abgenommene Anforderung ihre Grundlage verloren. `feldText` steht in genau
derselben Lage eine Datei weiter: **`cockpitFeldText` und `widgetZeile` verweisen in ihren
Docstrings ausdrücklich auf seine Formulierung als Vorbild** („die geschlossene Menge
entscheidet, nicht ihre Verneinung"), und `cockpitFeldText` trägt den Kommentar, dass genau
dieser Fehler drei Funktionen weiter oben beschrieben stand.

## Zu klären, bevor gebaut wird

1. **Ist `feldText` wirklich tot, oder ist es die Vorlage?** Zwei lebende Funktionen
   verweisen namentlich auf sie. Ein gelöschtes Vorbild macht zwei Kommentare unlesbar —
   und ein Kommentar, der auf nichts mehr zeigt, ist schlimmer als keiner
   (`L-2026-08-20bs`: *ein Kommentar altert nicht mit*).
2. **Löschen oder umdrehen?** `dashboardGruppen` trägt die Gegenprobe „eine **unbekannte**
   Gruppe verschwindet nicht" (SWR-096) — eine Regel, die über die Kacheln hinausgeht.
   Wird der Code gelöscht, muss gesagt werden, wo diese Regel künftig geprüft wird, oder
   sie fällt still weg. ⚠ Genau die Bauart, die SWR-125 gekostet hat.
3. **Was sagt der Zähltest?** `platform/tests/test_renderweg_zaehlung.py` führt
   `ROHTEXT_ANSICHTEN` als Bestandsmarke. Braucht die Kachelhälfte ihre eigene Marke, oder
   ist ihr Fehlen nach dem Löschen von allein sichtbar?

⚠ **Kein Aufräumen im Vorbeigehen.** Vier Bausteine und elf Zusicherungen zu entfernen ist
eine Änderung mit eigener Teststrecke. Wenn sie in einem Lauf nicht ganz zu tragen ist,
gehört sie nicht an dessen Ende — derselbe Satz, aus dem `T-0015` dreimal verschoben wurde
und der dort richtig war.

⚠ **Nullte Verschiebung** — dieses Ticket ist in Sprint 24 entstanden.

---

## Sprint 25: ERSTE Verschiebung — Grund benannt

Dieser Lauf ist vollständig in `platform/T-0029` gegangen (Auto-Push seit drei Tagen tot,
83 Abbrüche; 12 Ticks des Auftraggebers nie gelaufen) und in die Antworten auf zwei Briefe.
Der Rückbau der Frontend-Hälfte liefert nichts Neues und kann etwas kaputt machen — er ist
genau die Sorte Arbeit, die einen ganzen Lauf tragen muss und keinen Rest davon.

⚠ **Erste Verschiebung, Grund: Kapazität.** Der Umfang ist unverändert **gezählt** (4
Bausteine, 11 von 111 JS-Zusicherungen) und nicht neu geschätzt.

**Neuer Termin: Sprint 26.**

---

## Sprint 26: ZWEITE Verschiebung — Grund benannt, Umfang unverändert gezählt

Dieser Lauf ist in die Reparatur des Orchestrators gegangen (`platform/T-0031`,
`platform/T-0032`): die ersten drei Ticks, die je durchliefen, lieferten nichts und
hinterließen `main` und einen Feature-Branch divergiert.

⚠ **Warum das gerade diesen Rückbau trifft.** Sein eigener Text sagt: *„Vier Bausteine und
elf Zusicherungen zu entfernen ist eine Änderung mit eigener Teststrecke. Wenn sie in einem
Lauf nicht ganz zu tragen ist, gehört sie nicht an dessen Ende."* Ein Rückbau, der in einem
Lauf beginnt, in dem die Git-Schreibwege gerade repariert werden, ist genau der Rückbau,
der etwas kaputt macht — und diesmal wäre der Grund nicht einmal der Code gewesen.

⚠ **Zweite Verschiebung, Grund: Kapazität.** Der Umfang ist unverändert **gezählt** (4
Bausteine, 11 von 111 JS-Zusicherungen) und nicht neu geschätzt. Die drei Vorabfragen sind
unverändert unbeantwortet — sie sind der eigentliche Arbeitsvorrat, nicht das Löschen.

**Neuer Termin: Sprint 27.** ⚠ Bei der **vierten** Berührung gilt: gebaut oder geschnitten.

---

## Sprint 27: DRITTE Verschiebung — Kapazität, und der Umfang bleibt GEZÄHLT statt neu geschätzt

**Was Sprint 27 stattdessen getan hat, nachzählbar:** vier Anforderungen geschlossen
(`SWR-171`–`SWR-174`), zwei Tickets geschlossen (`platform/T-0033` kritisch,
`platform/T-0027` bei der vierten Berührung), ein Ticket abgetrennt (`platform/T-0034`)
und ein DR gestellt (`platform/T-0035`).

**Umfang unverändert, aus dem Ticketkopf übernommen und nicht neu geraten:** 4 Bausteine,
**11** von 111 JS-Zusicherungen. ⚠ Diese 11 sind selbst der **achte Beleg** von
`platform/T-0027` (am Bildschirm abgezählt: 9; über einen Durchlauf gemessen: 11) — und
`SWR-173/174`, in diesem Sprint gebaut, hätte sie **nicht** gefunden: sie decken die
wiederkehrenden Kennzahlen ab, nicht die einmalige Zählung eines einzelnen Berichts. Das
steht so im Abschluss von `T-0027` und wird hier nicht schöner gemacht.

⚠ Die drei offenen Fragen im Kopf (*ist `feldText` tot oder ist es die Vorlage?* ·
*löschen oder umdrehen?* · *braucht die Kachelhälfte eine eigene Bestandsmarke?*) sind
**unbeantwortet** und werden nicht im Vorbeigehen beantwortet — ein Rückbau, der während
seiner Ausführung wächst, ist genau der Rückbau, der etwas kaputt macht (`p11/T-0015`).

**Termin: Sprint 28**, Rolle `dev`. ⚠ **Dritte Verschiebung.** Bei der vierten gilt:
gebaut oder geschnitten.

---

## Sprint 28: VIERTE BERÜHRUNG — gebaut UND geschnitten, und die Messung hat Frage 1 UMGEDREHT

**Die drei Vorabfragen sind nicht im Vorbeigehen beantwortet worden, sondern zuerst und
durch Zählen** — die Regel dieses Hauses (erst zählen, dann bauen).

### Frage 1: *„ist `feldText` tot oder ist es die Vorlage?"* — die Frage bot zwei Antworten an, gemessen sind es **beide**

| Baustein | Code-Aufrufer (gemessen) | eigene JS-Zusicherungen | Verweise aus lebendem Code |
|---|---|---|---|
| `kachelFelder` | **0** (nur der eigene Export) | 2 Blöcke | 0 |
| `dashboardGruppen` | **0** (nur der eigene Export) | 4 Blöcke | 0 |
| `FELD_TITEL` | 1 — **im toten `kachelFelder`** | 0 | 0 |
| `feldText` | 1 — **im toten `kachelFelder`** | **7 Blöcke** | **3 lebende Funktionen zitieren ihn namentlich** (Zeilen 505/512/519/539) |

> **⚠⚠ `feldText` ist totes Aufrufziel UND lebende Vorlage zugleich. Wer nur die Aufrufer
> zählt, löscht den zitierten Originaltext einer Vertragsregel, auf die drei lebende
> Funktionen zeigen — und merkt es nicht, weil danach alles grün ist.**

### Frage 2: *„löschen oder umdrehen?"* — **beides**, und die Auflage der Frage ist eingelöst

Die Frage stellte eine **Bedingung**: *„Wird der Code gelöscht, muss gesagt werden, wo die
`SWR-096`-Regel künftig geprüft wird, oder sie fällt still weg — genau die Bauart, die
`SWR-125` gekostet hat."* Sie wird hier nicht übergangen, sondern **nachgesehen**:

| Wo `SWR-096` heute geprüft wird | Zustand |
|---|---|
| `regeln.test.cjs:187` — *„eine Aufgabe ohne Rolle bekommt eine BENANNTE Gruppe statt zu fehlen (SWR-096)"* | **lebendig**, an `OHNE_ROLLE`/`aufgabenNachRolle` |
| `aggregation._zustand` (`SWR-096/108`), `widgets.py`, `workproducts.py` | **lebendig**, drei Vertragszustände |
| `dashboardGruppen` „unbekannte Gruppe → `sonstige`" | **war die einzige Fassung an totem Code** |

> **Die Regel hängt nicht an der gelöschten Prüfung. Sie hatte mehrere Vertreter, und
> gegangen ist der, der an nichts mehr hing — das ist der Unterschied zu `SWR-125`, wo
> eine Regel ohne jeden Vertreter zurückblieb. Nachgesehen, nicht angenommen.**

### Frage 3: *„braucht die Kachelhälfte eine eigene Bestandsmarke?"* — **nein**, gemessen

`test_renderweg_zaehlung.py` führt `ROHTEXT_ANSICHTEN = 4` als Bestandsmarke für die
**Rohtext-Ansichten**, nicht für Kachelbausteine; die Zahl ist von diesem Rückbau
unberührt und der Test bleibt grün. Die Abwesenheit der drei Bausteine ist ab jetzt durch
das **Zusicherungspaar** selbst sichtbar — eine zweite Marke wäre eine zweite Aussage über
dieselbe Sache (B033).

### Ausgeführt

**Entfernt:** `kachelFelder`, `dashboardGruppen`, `FELD_TITEL` aus `regeln.js` samt ihren
Exporten (2108 Zeichen). **Geblieben:** `feldText`, `KEINE_DATEN`.

**Zusicherungen UMGEDREHT statt gelöscht** (Bauform aus `SWR-148`/`SWR-135` v1.61): 6
Testblöcke mit 12 `assert`-Zeilen entfernt, 2 Blöcke mit 7 Zeilen neu — als **Paar**, weil
eine Prüfung, die nur Abwesenheit misst, nach einem Kahlschlag ebenfalls grün ist.

### ⚠ Und beim Ausführen ist eine Zahl dieses Tickets gefallen

Der Kopf trug seit Sprint 24 **„11 von 111 JS-Zusicherungen"**, dreimal unverändert
weitergereicht und ausdrücklich als *„gezählt statt geschätzt"* geführt. Gemessen beim
Ausführen: **12** `assert`-Zeilen in **6** Blöcken — und **111 ist die TESTzahl**, die
Zusicherungen sind **240**.

> **Zwei Größen unter einem Namen. Die Zahl war gezählt, aber nicht die, die daneben
> stand — der neunte Beleg der `platform/T-0027`-Familie, und `SWR-173/174` fangen ihn
> nicht: sie decken die wiederkehrenden Kennzahlen ab, nicht die Einzelzählung eines
> Tickets.**

### Verifikation

JS-Teststrecke **111 → 107 grün** (6 entfernt, 2 neu), `node --check` grün für `regeln.js`
und `app.js`, 0 Restverweise im Produktivcode, `test_dashboard_endpunkt`/`test_backend`/
`test_cockpit_zustand`/`test_js_teststrecke` grün. Anforderung nachgezogen: **v1.74**
(`SWR-135` zweite und letzte Rückschneidung).
