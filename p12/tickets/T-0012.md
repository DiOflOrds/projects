---
id: T-0012
titel: "Sollen Ticket-Bodys und Dokumentenansichten wie Briefe gerendert werden? — die Frage, die fünfmal als Aufgabe terminiert wurde"
typ: decision-request
prozess: swe2
rolle: pl
sprint: 25
status: in_progress
prio: niedrig
blocked_by: []
repo: p12
optionen: [A, B, C]
frist: 2026-08-27
default: A
reviewer: qm
geändert: 2026-08-20
geplant_sprint: 26
erstellt: 2026-08-20
---

## Herkunft — herausgeschnitten aus `p12/T-0011` bei dessen Abschluss

`p12/T-0011` stand seit dem 17.08. und ist **fünfmal** angefasst worden. Jedes Mal war der
Grund „Kapazität", und jedes Mal war er wahr. Beim Hinsehen in Sprint 25 war der Grund ein
anderer:

> **In der Aufgabe steckte eine Frage, die niemandem gestellt worden ist — und der
> einzige, der sie beantworten kann, ist der, der die Ansichten liest.**

Das ist wörtlich der Fehler, den `platform/T-0020` vier Sprints lang gemacht hat, und die
Bewegung dagegen ist die von `promt-team/T-0008` in Sprint 24: **umschneiden statt ein
weiteres Mal terminieren**.

## Der Zustand — gemessen, mit Vertreter in der Prüfung

Vier Ansichten zeigen Rohtext im `<pre>` und laufen **nicht** über den Block-Renderer.
Seit Sprint 25 sind sie **einzeln benannt** und nicht mehr nur gezählt
(`test_renderweg_zaehlung.RohtextAnsichtTest.ROHTEXT_STELLEN`):

| Ansicht | erkannt an |
|---|---|
| Ticket-Body | `preMitLinks(t.body, …)` |
| DR-Body | `preMitLinks(dr.body, …)` |
| Dokumentenansicht (`dateiKarten`) | `preMitLinks(d.text, projekt)` |
| Requirements-Ansicht | `preMitLinks(d.text, d.projekt …)` |

Ihre **Inline**-Regeln (`**fett**`, `` `code` ``, `T-nnnn`) kommen seit Sprint 19 aus dem
einen Inline-Pass. Was fehlt, ist der **Block**-Teil: `## Ziel` bleibt eine Zeile mit zwei
Rauten, `| a | b |` bleibt eine Zeile mit Strichen.

## Die Frage an den Auftraggeber

**Option A — so lassen (Empfehlung).** Zwei Darstellungsgrade bleiben: Briefe voll
gerendert, Ticket-Bodys und Dokumente roh. Der Grund für A ist nicht Bequemlichkeit: im
Rohtext bleiben die **Frontmatter-Zeilen** sichtbar (`status`, `rolle`, `sprint`, `frist`),
und die sind beim Lesen eines Tickets oft das Wichtigste. Ein Renderer macht sie weg oder
verlangt eine Sonderbehandlung, die es heute nicht gibt.

**Option B — umstellen.** Überschriften und Tabellen im Ticket sehen aus wie im Brief.
⚠ Der Preis ist benannt und nicht klein: (1) die Frontmatter-Zeilen brauchen eine eigene
Darstellung, (2) der bestehende Vollständigkeitsnachweis misst **57 Briefe** und sagt über
Ticket-Bodys nichts — *ein Nachweis über den falschen Bestand ist grün und sagt nichts*;
er müsste über Ticket-Bodys und Requirements-Dokumente neu geführt werden.

**Option C — nur die Dokumentenansichten umstellen**, Ticket- und DR-Body roh lassen. Damit
liefe die Requirements-Ansicht (viele Tabellen) gerendert, und die Frontmatter-Frage stellt
sich gar nicht.

## Wie entschieden wird

⚠⚠ **Der erste Entwurf dieses Tickets stand daneben, und das Werkzeug hat ihn widerlegt.**
Er sagte, die Frage gehöre *nicht* in die Inbox — die sei für Klasse A — und sie werde
stattdessen im Bericht gestellt. `board.py` hat den Entwurf abgewiesen (`decision-request`
ohne `optionen`), und beim Nachsehen in `inbox.py` steht: **jeder offene
`decision-request` landet in der Inbox**, ohne Ansehen der Klasse.

> **Ein Ticket, das beschreibt, wie es behandelt werden will, bestimmt nicht, wie es
> behandelt wird. Der Satz hätte drei Sprints lang das Gegenteil des Codes behauptet —
> derselbe Fall wie SWR-162.**

Also: **Die Frage geht in die Inbox**, als Klasse C, mit drei Knöpfen.

⚠⚠ **Und auch der zweite Entwurf ist von einem Werkzeug widerlegt worden — im selben
Lauf.** Er wollte **keine Frist** setzen, mit dem Argument, eine Darstellungsfrage dürfe
nicht drängen. Der Preflight hat ihn als **unterminiertes Ticket** gemeldet, und die
Begründung dafür steht in `aggregation.unterminierte_tickets`: *„Ein DR ohne `frist` ist
weiterhin unterminiert."* Das ist kein Formalismus — es ist die Regel, die dafür sorgt,
dass **Schweigen ein definiertes Ergebnis hat** statt einer Frage, die ewig offen steht.

> **Eine Frage ohne Frist ist keine schonende Frage. Sie ist eine, deren Ausgang niemand
> aufgeschrieben hat.**

**Frist: 2026-08-27. Default: A = heutiger Zustand.** Damit drängt sie trotzdem nicht:
Verstreicht die Frist, gilt A, und A ist genau das, was ohnehin läuft. Schweigen kostet
nichts und baut nichts zurück.

⚠ Sie erhöht damit den Zähler „auf dich wartende Entscheidungen" um eins. Das ist der
Preis dafür, sie überhaupt zu stellen, und er ist kleiner als der bisherige: fünfmal
terminiert, nie gefragt.

## DoD

Antwort da → Umsetzung als eigenes Ticket mit dem in Option B genannten
Vollständigkeitsnachweis. Frist verstreicht → Default **A** greift, das Ticket wird mit dem
Vermerk geschlossen, dass A galt und gilt, und es wird **nichts** zurückgebaut.

**Entscheidung (D004, via Inbox, 2026-08-20 20:34):** A

---

## Sprint 26: VERBUCHT — und die Antwort kam mitten in diesen Lauf hinein

Der Auftraggeber hat am **2026-08-20 um 20:34** über die Inbox mit **A** entschieden
(`D004`, `projects/p12/management/decisions/decision-log.md`). Sprint 26 hatte um **20:29**
begonnen — die Frage stand **fünf Minuten**, nachdem sie fünf Sprints lang als Aufgabe
terminiert worden war.

> **Fünfmal „keine Zeit", einmal gefragt, fünf Minuten Antwortzeit.** Der Aufwand lag nie
> im Beantworten. Er lag darin, die Frage überhaupt als Frage zu erkennen.

**Was A bedeutet und was daraus folgt:** Es bleibt beim heutigen Zustand — Briefe voll
gerendert, Ticket-Bodys und Dokumentenansichten als Rohtext, damit die Frontmatter-Zeilen
(`status`, `rolle`, `sprint`, `frist`) beim Lesen sichtbar bleiben. **Es wird nichts
gebaut und nichts zurückgebaut.** Die vier Rohtext-Ansichten bleiben, wo sie sind, und
`RohtextAnsichtTest.ROHTEXT_STELLEN` benennt sie seit Sprint 25 einzeln — die Prüfung
merkt ab jetzt, wenn sich eine davon still ändert. Das ist die gesamte Folgearbeit.

⚠ **Kein Nachfolgeticket.** Die DoD sah eines für den Fall B vor; bei A gibt es keinen Bau,
und ein Ticket „nichts tun" wäre ein Vorgang ohne Gegenstand.

### ⚠ Gefunden hat die Entscheidung nicht der Preflight, sondern die Teststrecke

Der Preflight des letzten Schnelltakt-Ticks (20:15) meldete *„Entschiedene, unverbuchte
DRs: 0"* — korrekt, denn die Antwort gab es da noch nicht. Bemerkt hat sie
`test_dr_verbuchung.BestandTest.test_kein_entschiedener_dr_ist_unverbucht`, die während
dieses Laufs über den echten Bestand lief und rot wurde.

> **Eine Zusicherung über den echten Bestand ist der einzige Wächter, der etwas bemerkt,
> das zwischen zwei Läufen passiert. Die rote Zeile war keine Störung des Laufs — sie war
> sein Ergebnis.**
